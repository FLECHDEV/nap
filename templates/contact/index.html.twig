{% extends 'base.html.twig' %}

{% block title %}NAP-Contact{% endblock %}

{% block body %}
    {% for message in app.flashes('message') %}
        <div class="alert alert-success" role="alert">{{ message }}</div>
    {% endfor %}
    <h1>Oui ?</h1>
    {{ form(contactForm) }}
{% endblock %}
