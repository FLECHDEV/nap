    {% if app.user %}
        <div class="mb-3 header">
            salut  {{ app.user.username }}, <a class="deconnexion" href="{{ path('app_logout') }}">Déconnexion</a>
        </div>
    {% endif %}

    {% for message in app.flashes('message') %}
        <div class="alert alert-secondary" role="alert">{{ message }}</div>
    {% endfor %}