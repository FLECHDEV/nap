{% extends 'base.html.twig' %}

{% block title %}Hello DialogueController!{% endblock %}

{% block body %}
<h2>Nos conversations</h2>
    <table class="table table-striped table-dark">
        <thead>
            <tr>
                <th scope="col">Pseudo</th>
                <th scope="col">Message</th>
                <th scope="col">Date</th>
            </tr>
        </thead>
        <tbody>
        {% for dialogue in dialogues %}
            <tr>
                <td>{{ dialogue.User.username}}</td>
                <td>{{ dialogue.contenu }}</td>
                <td>{{ dialogue.createdAt|date('d/m/y') }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <h2>Nouveau message</h2>
    {# {{ form(dialogueForm) }} #}
    
    {# <form>
        <div class="form-group">
            <label for="exampleFormControlTextarea1">Nouveau message</label>
            <textarea class="form-control" id="exampleFormControlTextarea1" rows="3"></textarea>
        </div>
        <button type="submit" class="btn btn-primary mb-2">Envoyer</button>
    </form> #}

{% endblock %}
